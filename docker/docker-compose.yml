services:
  server:
    build:
      context: ..
      dockerfile: docker/server.Dockerfile
    container_name: pixel-vpn-server
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 8787
      DATABASE_URL: mysql://pixelvpn:pixelvpn@mysql:3306/pixelvpn
      JWT_SECRET: ${JWT_SECRET:-change_this_to_a_long_random_string_1234567890}
      CORS_ORIGIN: ${CORS_ORIGIN:-*}
    ports:
      - "8787:8787"
    networks:
      - vpn

  mysql:
    image: mysql:8.4
    container_name: pixel-vpn-mysql
    restart: unless-stopped
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: pixelvpn
      MYSQL_USER: pixelvpn
      MYSQL_PASSWORD: pixelvpn
    ports:
      - "3307:3306"
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -proot --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - vpn

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: pixel-vpn-phpmyadmin
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
      PMA_USER: root
      PMA_PASSWORD: root
      UPLOAD_LIMIT: 128M
    ports:
      - "8081:80"
    networks:
      - vpn

  xray:
    image: ghcr.io/xtls/xray-core:latest
    container_name: pixel-vpn-xray
    restart: unless-stopped
    ports:
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./xray/config.json:/etc/xray/config.json:ro
    networks:
      - vpn

networks:
  vpn:
    driver: bridge

volumes:
  mysql-data:
